language: c
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get install -y bc
install:
  - cd src; ./autogen.sh; cd -
  - make -C src
env:
  - CONNLM_NO_OPT=1
script: 
  - source tools/shutils/shutils.sh
  - cd egs/tiny; shu-testing

